public String divide(Integer a, Integer b){
    //code here
    try {
        Integer c = a/b;
        return String.valueOf(c);
    } catch(exception ex){
        if(a == null || b == null) return ex.message;
        return 'Divide by 0';
    } 
}