public List<Integer> evenNumbers(Integer n) {
    //code here
    List<Integer> evenNums = new List<Integer>();
    Integer num = 2;
    while(evenNums.size()<n){
        if(Math.mod(num,2) == 0) evenNums.add(num);
        num++;
    }
    return evenNums;
}