public Id insertStudent(String name, String email) {
    // code here
    apxio__Student__c student = new Student(
        Name = name, apxio__Email__c = email
    );

    insert student;
    return student.Id;
}